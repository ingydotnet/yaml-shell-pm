<<<cpan-head>>>

= Synopsis

  > ysh [options]

= Description

This module supports the `ysh` command. It is not to be used in any general
way as a Perl module.

See [ysh] for detailed information on using the `ysh` command.

<<<cpan-tail>>>
